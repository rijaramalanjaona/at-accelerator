import { ToogleFavoriteDirective } from './toogle-favorite.directive';

describe('ToogleFavoriteDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleFavoriteDirective();
    expect(directive).toBeTruthy();
  });
});
